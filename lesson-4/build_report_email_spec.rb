require "spec_helper"

RSpec.describe "BuildReportEmail" do
  context "text method" do
    it "should have proper subject" do
    end

    it "should include error links when there are error" do
    end

    it "should not include error links when there are no errors" do
    end

    it "should include success message when there are no errors" do
    end
  end

  context "#deliver" do
    it "calls mail#deliver_to() method" do
    end
  end
end
