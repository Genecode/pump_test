require "spec_helper"

RSpec.describe "User" do
  describe ".create_from_sso_provider" do
    it "updates his/her name when user with such uid already exists" do
    end

    it "creates user with provided attributes when user with such uid doesn't exist" do
    end
  end
end
